<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: Inter;
    }

    .total-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding-top: 15em;
    }

    .add-container {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .heading {
      font-size: 25px;
      padding: 5px;
      margin: 5px;
      width: 376px;
      color: black;
      font-weight: bold;
      border: dotted #2A85C7;
    }

    .text-area {
      font-size: 20px;
      padding: 5px;
      margin: 5px;
      color: black;
      border: dotted #2A85C7;
    }

    .button {
      font-size: 20px;
      color: #194F75;
      background-color: white;
      border: dotted;
      border-color: #194F75;
      padding: 5px;
      margin: 5px;
      width: 390px;
      font-weight: bold;
    }

    .go-back {
      text-decoration: none;
      display: flex;
      justify-content: center;
      font-size: 20px;
      color: #194F75;
      background-color: white;
      border: dotted;
      border-color: #194F75;
      padding: 5px;
      margin: 5px;
      width: 380px;
      font-weight: bold;
    }
  </style>
  <title>Add new note</title>
</head>

<body>
  <div class="total-container">
    <a href="/notes" class="go-back">GO BACK </a>
    <form action="/notes/<%= note._id %>?_method=PUT" method="POST" class="add-container">
      <input type="text" name="heading" value="<%= note.heading %>" class="heading" required>
      <textarea name="description" id="" cols="30" rows="10"
        class="text-area"> required<%= note.description %></textarea>
      <button class="button">UPDATE</button>
    </form>
    <form action="/notes/<%= note._id %>?_method=DELETE" method="POST">
      <button class="button">DELETE</button>
    </form>
  </div>
</body>

</html>