<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update todo</title>
  <style>
    <style>body {
      font-family: Inter;
    }

    .total-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding-top: 15em;
    }

    .add-container {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .heading {
      font-size: 25px;
      padding: 5px;
      margin: 5px;
      width: 376px;
      color: black;
      font-weight: bold;
      border: dotted #2A85C7;
    }

    .text-area {
      font-size: 20px;
      padding: 5px;
      margin: 5px;
      color: black;
      border: dotted #2A85C7;
    }

    .button {
      font-size: 20px;
      color: #194F75;
      background-color: white;
      border: dotted;
      border-color: #194F75;
      padding: 5px;
      margin: 5px;
      width: 390px;
      font-weight: bold;
    }

    .go-back {
      text-decoration: none;
      display: flex;
      justify-content: center;
      font-size: 20px;
      color: #194F75;
      background-color: white;
      border: dotted;
      border-color: #194F75;
      padding: 5px;
      margin: 5px;
      width: 380px;
      font-weight: bold;
    }
  </style>
  </style>
</head>

<body>
  <div class="total-container">
    <a href="/today" class="go-back">GO BACK</a>
    <form action="/today/<%= todo._id %>?_method=PUT" method="POST" class="add-container">
      <input type="text" name="title" value="<%= todo.title %>" class="heading" required>
      <textarea name="description" id="description" cols="30" rows="10" class="text-area"
        required><%= todo.description %></textarea>
      <button class="button">UPDATE</button>
    </form>
  </div>
</body>

</html>